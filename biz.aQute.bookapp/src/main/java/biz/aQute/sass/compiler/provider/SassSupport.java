package biz.aQute.sass.compiler.provider;

import java.util.Formatter;

import biz.aQute.book.Generator;

public class SassSupport {

	public static void compile(Generator generator, Appendable sw) {
		try (Formatter f = new Formatter(sw)) {

		}
	}
}
